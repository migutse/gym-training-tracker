{
  "name": "gym-training-tracker",
  "version": "1.0.0",
  "description": "Full-stack gym training session tracker",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "start": "npm start --workspace=server"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19"
  }
}
